<h1> Class List: </h1>
<ul>
<% @school_classes.each do |sc| %> 
<div><%= link_to sc.title, school_class_path(sc) %> <%= sc.room_number %> </div>
</ul>
<% end %> 

<%= link_to "Create A Class", new_school_class_path %> 